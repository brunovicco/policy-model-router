# ADR 0013 — Continue W3C trace context at the routing enforcement boundary (renumbered)

Status: Superseded by numbering — see
[ADR-0016](0016-w3c-runtime-trace-context.md).

This decision was accepted with a number already taken by
[ADR-0013 — Structured runtime violation evidence](0013-structured-runtime-violation-evidence.md).
In a repository whose proposition is decision traceability, the ADR number is the primary key, so
the tracing decision was renumbered to 0016. This stub remains so existing links keep resolving;
the content lives at ADR-0016 and is unchanged.
